# Simple leader traits to avoid errors

leader_traits = {
	# Most basic cavalry leader trait possible
	cavalry_leader = {
		allowed = {
			has_unit_leader_flag = is_field_marshal
			has_unit_leader_flag = is_general
		}
		
		modifier = {
		}
	}
}